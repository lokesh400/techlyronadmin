<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <script src="https://cdn.tailwindcss.com"></script>
  <title>Proposal Response</title>
</head>
<body class="bg-gray-100 p-6 sm:p-10">
  <div class="max-w-xl mx-auto bg-white p-8 shadow">
    <% if (status === 'accepted') { %>
      <h1 class="text-2xl font-bold text-green-700 mb-2">Proposal Accepted</h1>
      <p class="text-gray-700">Thank you for accepting the proposal.</p>
      <% if (lead) { %>
        <p class="mt-4 text-gray-600">Client: <strong><%= lead.name || 'Client' %></strong></p>
      <% } %>
    <% } else if (status === 'rejected') { %>
      <h1 class="text-2xl font-bold text-red-700 mb-2">Proposal Rejected</h1>
      <p class="text-gray-700">Your response has been recorded.</p>
      <% if (lead) { %>
        <p class="mt-4 text-gray-600">Client: <strong><%= lead.name || 'Client' %></strong></p>
      <% } %>
    <% } else if (status === 'not-found') { %>
      <h1 class="text-2xl font-bold mb-2">Link Not Found</h1>
      <p class="text-gray-700">This proposal link is invalid or has expired.</p>
    <% } else { %>
      <h1 class="text-2xl font-bold mb-2">Something Went Wrong</h1>
      <p class="text-gray-700">Please try again later.</p>
    <% } %>
  </div>
</body>
</html>
